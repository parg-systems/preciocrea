{
  "name": "preciocrea",
  "version": "2.3.1",
  "private": true,
  "description": "Calculadora de precios para emprendedoras creativas (PWA). Este package.json existe SOLO para los tests.",
  "scripts": {
    "test": "node --test tests/",
    "test:e2e": "playwright test",
    "test:todo": "node --test tests/ && playwright test",
    "servidor": "node tests/e2e/servidor.js",
    "portable": "node scripts/build-portable.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "jsdom": "^26.0.0"
  }
}
